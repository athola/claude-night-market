---
name: gamma
description: Planted skill-graph control. Nothing calls it and it calls nothing.
---

# Gamma

The isolate the audit must report.

## Exit Criteria

- [ ] `tests/scripts/test_skill_graph.py -k Planted` reports the
      fixture tree's known answer: one bug-class dangling reference,
      one isolate, two edges
