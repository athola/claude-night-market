[project]
name = "minister"
version = "1.9.19"
description = "GitHub-centric project management plugin for Claude Code"
authors = [{name = "Claude Night Market"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.14.13",
    "mypy>=1.11.0,<2",
    "pytest>=8.0",
    "pytest-cov>=4.1",
]

[tool.nightmarket]
coverage_threshold = 90

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/minister"]

[tool.ruff]
# Inherit the repo-wide rule floor; this file may only add.
extend = "../../pyproject.toml"
line-length = 88
target-version = "py39"
exclude = [".venv", ".uv-cache"]

[tool.ruff.lint]
extend-select = ["E", "W", "F", "I", "B", "C4", "UP", "S", "PL", "D"]
extend-ignore = [
    "S101",   # Use of assert (acceptable)
    "E203",
    "D203",
    "D213",
    "D107",
    "S603",
    "S607",
    "E501",   # Line too long (handled by formatter); matches root config
]

[tool.ruff.lint.extend-per-file-ignores]
"tests/**/*.py" = [
    "PLR2004",  # Magic values in test assertions are acceptable
    "S311",     # Allow pseudo-random generators in tests
]
"scripts/tracker.py" = [
    "E402",  # sys.path bootstrap must run before the deferred import
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
strict = true

# leyline is a soft dependency: it holds the shared plugin-data
# resolution rule, but its src is not on sys.path when minister runs
# from an install tree, so project_tracker imports it under a guard and
# falls back. Mirrors memory-palace's override for the same reason.
[[tool.mypy.overrides]]
module = ["leyline", "leyline.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --cov=minister --cov-report=term-missing"
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "slow: Slow running tests",
]

[tool.coverage.run]
source = ["src/minister"]
omit = ["*/tests/*", "*/__pycache__/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
