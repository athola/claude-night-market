## PR Review #446: 1.9.3 Release

**Scope mode**: standard
**Reviewer**: automated /pr-review
**CI**: 5/5 SUCCESS (sync-check, build, ruff UP007, security-scan, slop-check)
**Verdict**: Approve with hygiene notes

### Scope Assessment

The PR closes #445 (inclusive defaults audit). The body
describes 4 reviewable commits, but the branch has 29 commits
and 349 changed files since divergence from master. The branch
has effectively become the 1.9.3 release.

**Coherent additions** (in-scope for 1.9.3):

- Inclusive-defaults policy + 3 targeted flips (the original
  4 commits)
- Skill audit follow-ups + `skill-graph-audit` (b71bd49d,
  42f7ce84)
- Pensive `/performance-review` skill + command (e2a4ba4e)
- Gauntlet Makefile demos + hook fixes (b58e9d40, 9bfc0a7a,
  83b3d3ed)
- Pensive perf cleanups from dogfooding (23277c8c)
- 1.9.3 README / CHANGELOG (f3f8de79) + version bump
  (22423bca)

**Recommendation**: classify as 1.9.3 release PR rather than
single-feature PR. Update PR description to reflect actual
scope.

### Hygiene Findings

| # | Finding | Severity | Action |
|---|---------|----------|--------|
| 1 | PR body says "four reviewable commits" but branch has 29 | Non-blocking | Update PR description (done as part of this review) |
| 2 | Commit `bdf77595` is labeled `chore(1.9.4):` on 1.9.3 branch | Non-blocking | Verify intentional vs. cherry-pick misfire |
| 3 | 178 SKILL.md files have `version:` frontmatter; only 1 (`gauntlet-curate`) has a parity test against plugin version | Out-of-scope | File for future cleanup |
| 4 | `update_versions.py` does not scan SKILL.md frontmatter despite slash-command docs claiming it does | Out-of-scope | File as bug |

### Version Consistency

- All 23 plugins on 1.9.3 (`jq '.version' plugins/*/.claude-plugin/plugin.json`).
- `uv.lock` workspace member versions synced (regenerated by pre-commit on the release commit).
- README badge + headline counts match disk: 185 skills, 128 commands, 23 plugins, 54 agents.
- `gauntlet-curate/SKILL.md` frontmatter version synced (caught by `test_skill_version_matches_plugin`).

### Code Quality (release commit `22423bca`)

Iron Law followed for `CACHE_EXCLUDES` fix:

- RED: failing test asserting `.typecheck-venv`, `.uv-tools`, `.xdg-cache`, `.tools` in exclusions
- GREEN: 4 entries added to `constants.py`; 14/14 tests pass
- Effect: `update_versions.py` dry-run noise dropped from 137 to 97 files (eliminated installed-package match risk)

Command-counting bug fixed at all 15 sites:

- 14 per-plugin `make status` Makefile targets + 1 doc-updates accuracy module
- Replaced `find commands/ -name '*.md'` with `find commands/ -maxdepth 1 -name '*.md'`
- Per-plugin `make status` now matches `plugin.json.commands.length` for all 23 plugins (verified on sanctum, imbue, spec-kit, leyline)
- Eliminates the false 155 → corrected 128 total command count

Documentation accuracy fixes are surgical (number-only edits to README headline/catalog, table-row additions to `book/src/plugins/pensive.md`).

Pre-existing regression test for gauntlet hook (`test_returns_none_when_challenges_unimportable`) was committed alongside, guarding the lazy ImportError handling.

### Documentation Quality

Slop scan: clean. README has 1 em dash in 1688 words, zero
tier-1 markers, zero tier-3 phrase patterns. CI's `slop-check`
also SUCCESS.

### Out-of-Scope Items (backlog candidates)

1. `update_versions.py` SKILL.md frontmatter scanning gap
2. SKILL.md version-parity test coverage (only 1 of 178 files guarded)
3. Audit/quality-gates orchestration docs (added in this PR but warrant their own follow-up review)

### Verdict

**Approve with notes**. The branch is the 1.9.3 release. CI
passes, version chain is coherent, the release commit followed
Iron Law for both bug fixes, and the four mandatory release
gates are green. Cosmetic items (PR body, mislabeled commit)
can be addressed inline.
