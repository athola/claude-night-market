# Bloat Detection Report — Tier 3

**Scan Date**: 2026-05-01
**Branch**: ai-slop-1.9.4
**Scope**: All first-party files (1,345 Python, 2,122 Markdown)
**Tools**: git-log, rg, wc, stat, ast.parse, plugin.json analysis
**Excluded**: .venv/, node_modules/, __pycache__, .git, htmlcov,
site-packages, .uv-tools, .typecheck-venv

---

## Summary

**Total findings**: 18 (6 HIGH, 7 MEDIUM, 5 LOW)

**Estimated token savings**: ~60,000 tokens
(computed at ~4 chars/token across flagged file bytes)

**Context reduction**: ~5–8% of working-tree footprint
for files routinely read by tools and agents

### Top 5 highest-impact targets

1. `node_modules/` — 509 MB on-disk, gitignored but
   physically present; a single dependency
   (`@google/gemini-cli`) with no invocations outside
   `plugins/conjure` docs; never appears in Makefile,
   CI, or pre-commit.
2. `coverage.xml` — 73 KB, gitignored, untracked, but
   present at repo root; generated artifact that bleeds
   into any context scan reading `.xml` globs.
3. `docs/tome-specification.md` + `docs/tome-implementation-plan.md`
   + `docs/tome-project-brief.md` — 2,012 lines combined,
   tracked in git despite matching the `.gitignore` pattern
   `docs/specification.md` / `docs/implementation-plan.md`
   / `docs/project-brief.md`; all three are pre-ship
   planning docs that duplicate content already in
   `plugins/tome/README.md`.
4. `scripts/add_cross_framework_fields.py` (536 lines,
   17 KB) + `scripts/add_model_hints.py` (152 lines,
   4.8 KB) + `scripts/cursor_lean_export.py` (390 lines,
   12 KB) — three one-shot migration/export scripts with
   zero references in Makefile, CI workflows, or
   pre-commit; no callers exist outside their own
   test files.
5. `scripts/stargazer_overlap.py` — referenced in
   `Makefile:151` (`make stargazer-analysis`) but the
   file does not exist; running the target produces an
   error.

---

## Findings

| ID | Path | Action | Risk | Confidence | Token Impact | Rationale |
|----|------|--------|------|-----------|--------------|-----------|
| F01 | `node_modules/` (509 MB) | DELETE | LOW | 88 | ~0 tokens (binary) | Gitignored; only depedency is `@google/gemini-cli` for conjure plugin docs; never invoked in Makefile/CI/pre-commit; safe to rm -rf and reinstall on demand |
| F02 | `coverage.xml` (73 KB) | DELETE | LOW | 99 | ~4,600 tokens | Gitignored (`.gitignore:43`), untracked; generated by `make test`; presence at repo root bleeds into XML-glob context scans |
| F03 | `htmlcov/` (1.6 MB) | DELETE | LOW | 99 | ~500 tokens | Gitignored (`.gitignore`); generated coverage HTML report; `htmlcov/.gitignore` exists inside it confirming intent to exclude |
| F04 | `__pycache__/` (root) | DELETE | LOW | 99 | ~50 tokens | Gitignored per `.gitignore:7`; stale `.pyc` for `conftest.py`; untracked; no reason to keep |
| F05 | `docs/tome-specification.md` (879 lines) | ARCHIVE | LOW | 90 | ~1,730 tokens | Tracked despite matching gitignore pattern for `docs/specification.md`; pre-ship draft dated 2026-03-20; content duplicates `plugins/tome/README.md` overview |
| F06 | `docs/tome-implementation-plan.md` (840 lines) | ARCHIVE | LOW | 90 | ~1,540 tokens | Tracked despite matching gitignore for `docs/implementation-plan.md`; dated 2026-03-20; describes work that shipped in v1.6.0 |
| F07 | `docs/tome-project-brief.md` (293 lines) | ARCHIVE | LOW | 90 | ~630 tokens | Tracked despite matching gitignore for `docs/project-brief.md`; superseded by `plugins/tome/README.md` |
| F08 | `scripts/add_cross_framework_fields.py` (536 lines) | ARCHIVE | MEDIUM | 85 | ~1,075 tokens | Zero references in Makefile, CI, or pre-commit; last committed 2026-03-28 as part of "refactor" batch; one-shot field-migration script; tests exist but no active caller |
| F09 | `scripts/add_model_hints.py` (152 lines) | ARCHIVE | MEDIUM | 85 | ~300 tokens | Zero references in Makefile, CI, or pre-commit; last committed 2026-03-28; one-shot model_hint migration script; tests exist but no active invocation path |
| F10 | `scripts/cursor_lean_export.py` (390 lines) | ARCHIVE | MEDIUM | 82 | ~770 tokens | Zero references in Makefile, CI, pre-commit, or any docs outside its own file; last committed 2026-03-28; no tests; Cursor integration path not used |
| F11 | `scripts/parse-doc-sync.py` (463 lines) | INVESTIGATE | MEDIUM | 75 | ~960 tokens | Zero references in Makefile, CI, or pre-commit; last committed 2026-03-01; only mention is inside the file's own docstring header |
| F12 | `scripts/lsp-diagnostic.sh` (266 lines) | ARCHIVE | LOW | 80 | ~585 tokens | Documented only in `docs/guides/lsp-native-support.md` (Jan 2026); not in Makefile, CI, or pre-commit; pure developer diagnostic with no CI gate |
| F13 | `scripts/lsp-vs-grep-comparison.sh` (235 lines) | ARCHIVE | LOW | 80 | ~630 tokens | Same doc-only reference as F12; last committed 2026-03-08; no CI path |
| F14 | `scripts/test-lsp-manually.sh` (121 lines) | ARCHIVE | LOW | 78 | ~260 tokens | Same doc-only reference as F12; last committed 2026-04-01; no CI path |
| F15 | `Makefile:151` (`stargazer-analysis` target) | REFACTOR | HIGH | 99 | ~5 tokens (Makefile line) | References `scripts/stargazer_overlap.py` which does not exist; `make stargazer-analysis` will fail; broken since at least 2026-03-25 (file deleted in unbloat commit `a3f11323`) |
| F16 | `plugins/abstract/skills/validate-plugin-structure.md` (79 lines) | INVESTIGATE | MEDIUM | 72 | ~275 tokens | Not declared in `plugins/abstract/.claude-plugin/plugin.json` skills list; orphaned asset; last committed 2026-01-24 |
| F17 | `plugins/abstract/skills/hook-scope-guide.md` (275 lines) | INVESTIGATE | MEDIUM | 72 | ~530 tokens | Not declared in `plugins/abstract/.claude-plugin/plugin.json` skills list; orphaned asset; last committed 2026-04-04 |
| F18 | `.pre-commit-hook-makefile-dogfood.yaml` (12 lines) | INVESTIGATE | LOW | 70 | ~30 tokens | Defines a local pre-commit hook (`makefile-dogfood-check`) but is not referenced in any `repos:` block of `.pre-commit-config.yaml`; zero other references in repo; may be an un-activated hook config |

---

## Evidence

### F01 — node_modules/ (509 MB)

```
$ du -sh node_modules/
509M	node_modules/

$ cat package.json
{"dependencies": {"@google/gemini-cli": "^0.25.1"}}

$ rg -l "gemini" --glob "Makefile" --glob "*.sh" -g "!node_modules"
(no output — zero Makefile/shell invocations)

$ git check-ignore -v node_modules/
.gitignore:66:node_modules/
```

Ref count outside node_modules: 0 direct invocations.
The `conjure` plugin has skills referencing Gemini but
uses it via the Claude SDK delegation path, not via this
npm package.

### F02 — coverage.xml (73 KB)

```
$ stat -c "%s" coverage.xml
73749

$ git check-ignore -v coverage.xml
.gitignore:43:coverage.xml

$ git ls-files coverage.xml
(no output — untracked)
```

Generated by `pytest --cov`. Last modified 2026-04-21.
Not tracked, but present on disk and readable by any
`find .` or XML-glob tool invocation.

### F03 — htmlcov/ (1.6 MB)

```
$ du -sh htmlcov/
1.6M

$ git check-ignore -v htmlcov/
(via .gitignore pattern)

$ ls htmlcov/
class_index.html  coverage_html_cb_bcae5fc4.js
coverage_html_cb_dd2e7eb5.js  index.html ...
```

Generated HTML coverage report. Contains two generations
of CSS/JS (both `cb_bcae5fc4` and `cb_dd2e7eb5`
variants), indicating multiple `make test` runs
accumulated. Gitignored, untracked.

### F04 — __pycache__/ (root)

```
$ ls __pycache__/
conftest.cpython-312-pytest-9.0.2.pyc

$ git check-ignore -v __pycache__/
.gitignore:7:**/*/__pycache__/

$ git ls-files __pycache__/
(no output)
```

Single `.pyc` from root `conftest.py`. Gitignored.

### F05–F07 — docs/tome-*.md (2,012 lines combined)

```
$ git ls-files docs/tome-implementation-plan.md \
               docs/tome-project-brief.md \
               docs/tome-specification.md
docs/tome-implementation-plan.md
docs/tome-project-brief.md
docs/tome-specification.md

$ git check-ignore -v docs/tome-specification.md
(exit 1 — not ignored, but .gitignore contains:
  docs/specification.md
  docs/implementation-plan.md
  docs/project-brief.md
  — the tome-prefixed names are not covered by patterns)

$ git log --format="%ci %s" -1 -- docs/tome-specification.md
2026-04-02 00:39:04 feat(gauntlet): use enums for challenge types, consolidate docs

$ wc -l docs/tome-*.md
  840 docs/tome-implementation-plan.md
  293 docs/tome-project-brief.md
  879 docs/tome-specification.md
 2012 total
```

All three are pre-ship planning docs. Status line in
`tome-specification.md:7` reads `Status: Draft`.
`plugins/tome/README.md` supersedes the overview content.

### F08–F10 — Migration scripts (cursor_lean, add_model_hints, add_cross_framework)

```
$ rg "add_model_hints|add_cross_framework|cursor_lean_export" \
    Makefile .pre-commit-config.yaml .github/workflows/
(no output)

$ git log --format="%ci" -1 -- scripts/add_model_hints.py
2026-03-28 18:57:53

$ git log --format="%ci" -1 -- scripts/add_cross_framework_fields.py
2026-03-28 18:57:53

$ git log --format="%ci" -1 -- scripts/cursor_lean_export.py
2026-03-28 18:57:53
```

All three committed in the same "tier 3 unbloat" commit.
Tests reference them but no active invocation path exists
in build, CI, or pre-commit. `cursor_lean_export.py`
has no tests at all.

### F11 — parse-doc-sync.py (463 lines)

```
$ rg "parse.doc.sync|parse_doc_sync" \
    Makefile .pre-commit-config.yaml .github/
(no output)

$ git log --format="%ci %s" -1 -- scripts/parse-doc-sync.py
2026-03-01 20:18:22 feat(sanctum,ci): add capabilities sync tooling
```

Zero references outside the file itself. Last commit
predates 5 subsequent releases (v1.6.0 through v1.9.3).

### F12–F14 — LSP diagnostic scripts

```
$ rg "lsp-diagnostic|lsp-vs-grep|test-lsp-manually" \
    Makefile .pre-commit-config.yaml .github/
(no output)

$ rg "lsp-diagnostic" docs/guides/lsp-native-support.md
| `scripts/lsp-diagnostic.sh` | Full system health check |

$ git log --format="%ci" -1 -- scripts/lsp-diagnostic.sh
2026-03-08 15:22:08

$ wc -l scripts/lsp-diagnostic.sh scripts/lsp-vs-grep-comparison.sh
266  lsp-diagnostic.sh
235  lsp-vs-grep-comparison.sh
```

Referenced only in `docs/guides/lsp-native-support.md`
(last modified 2026-01-25). Not gated in CI or Makefile.
Developer-only diagnostic tools with no automated trigger.

### F15 — Makefile:151 broken reference to stargazer_overlap.py

```
$ cat Makefile | grep -n stargazer
151:	@uv run python scripts/stargazer_overlap.py --limit 50

$ ls scripts/stargazer_overlap.py
ls: cannot access 'scripts/stargazer_overlap.py': No such file or directory

$ git log --all --oneline -- scripts/stargazer_overlap.py
a3f11323 refactor: tier 3 unbloat -- delete 182 dead files (66K lines)
70414bf6 feat: add cross-framework bridge and enhance PR workflows
```

File was deleted in the large tier-3 unbloat commit
`a3f11323` but the Makefile target was not removed.
`make stargazer-analysis` will fail with a Python
`ModuleNotFoundError`.

### F16–F17 — Orphaned abstract skills

```
$ python3 -c "
import json
d = json.load(open('plugins/abstract/.claude-plugin/plugin.json'))
skills = [s.lstrip('./').replace('skills/', '') for s in d['skills']]
print('validate-plugin-structure' in skills,
      'hook-scope-guide' in skills)
"
False False

$ git log --format="%ci" -1 -- \
    plugins/abstract/skills/validate-plugin-structure.md
2026-01-24 11:26:37

$ git log --format="%ci" -1 -- \
    plugins/abstract/skills/hook-scope-guide.md
2026-04-04 10:39:01

$ wc -l plugins/abstract/skills/validate-plugin-structure.md \
         plugins/abstract/skills/hook-scope-guide.md
  79  validate-plugin-structure.md
 275  hook-scope-guide.md
```

Neither appears in `plugin.json` skills array. Claude Code
will not load them as registered skills. `hook-scope-guide.md`
was modified 2026-04-04 (recent) — investigate before
deletion.

### F18 — .pre-commit-hook-makefile-dogfood.yaml (unused hook config)

```
$ cat .pre-commit-hook-makefile-dogfood.yaml
- repo: local
  hooks:
    - id: makefile-dogfood-check
      ...

$ rg "makefile-dogfood|pre-commit-hook-makefile" \
    .pre-commit-config.yaml
(no output)

$ rg "makefile-dogfood" . -g "!node_modules" -g "!.git"
(no output)

$ git log --format="%ci %s" -1 -- .pre-commit-hook-makefile-dogfood.yaml
2026-01-28 21:47:18 feat: add safety-critical patterns
```

The file defines a `repo: local` pre-commit hook but
it is not referenced in `.pre-commit-config.yaml`'s
`repos:` list. The hook has never run.

---

## Recommendations

### Phase 1 — Quick wins (30 minutes, zero risk)

These are untracked or gitignored artifacts with no
active references. Remove them without hesitation.

1. **Delete `node_modules/`** (509 MB freed).
   `rm -rf node_modules/`
   Re-run `npm install` only if the Gemini CLI is
   actively needed for the `conjure` gemini-delegation
   skill.

2. **Delete `coverage.xml`** (73 KB freed).
   `rm coverage.xml`
   Add `coverage.xml` to `.gitignore` explicitly if
   not already covered (it is — line 43 — but the file
   predates git tracking; the gitignore rule was
   added later).

3. **Delete `htmlcov/`** (1.6 MB freed).
   `rm -rf htmlcov/`
   Regenerate with `make test` as needed.

4. **Delete `__pycache__/` (root)**.
   `rm -rf __pycache__/`
   Already gitignored; will regenerate on next test run.

5. **Fix Makefile:151** (F15 — broken `stargazer-analysis`
   target). Either delete the target or restore
   `scripts/stargazer_overlap.py` from git history
   (`git show a3f11323:scripts/stargazer_overlap.py`).

### Phase 2 — Structural cleanup (1–2 hours, low risk)

These require a branch + PR because they modify tracked
files.

6. **Archive/delete `docs/tome-*.md`** (F05–F07).
   The `.gitignore` patterns `docs/specification.md`,
   `docs/implementation-plan.md`, `docs/project-brief.md`
   do not cover the `tome-` prefixed variants. Either:
   (a) add patterns `docs/tome-*.md` to `.gitignore`
   and remove from tracking, or
   (b) delete the three files (content superseded by
   `plugins/tome/README.md`).

7. **Archive one-shot migration scripts** (F08–F10).
   Move `add_cross_framework_fields.py`,
   `add_model_hints.py`, and `cursor_lean_export.py`
   to a `scripts/archive/` subdirectory or delete them.
   Their tests in `tests/unit/` should be deleted or
   moved alongside.

8. **Resolve orphaned abstract skills** (F16–F17).
   For each of `validate-plugin-structure.md` and
   `hook-scope-guide.md`: either add them to
   `plugin.json` skills list (if they should be active)
   or delete (if they are superceded). The
   `hook-scope-guide.md` was modified recently;
   confirm intent before deletion.

9. **Resolve `.pre-commit-hook-makefile-dogfood.yaml`**
   (F18). If the Makefile dogfood hook is wanted, add it
   to `.pre-commit-config.yaml`'s `repos:` block. If not,
   delete the file.

### Phase 3 — Deep investigation (2–4 hours, verify before deleting)

10. **Assess `scripts/parse-doc-sync.py`** (F11).
    No callers found. Confirm it was fully superseded
    by `scripts/capabilities-sync-check.sh` before
    deleting. Check git log context: last commit was
    2026-03-01 `feat(sanctum,ci): add capabilities
    sync tooling` — likely superseded that same day.

11. **Assess LSP scripts** (F12–F14).
    `lsp-diagnostic.sh`, `lsp-vs-grep-comparison.sh`,
    `test-lsp-manually.sh` are developer tools
    documented only in `docs/guides/lsp-native-support.md`
    (Jan 2026). They are not gated in CI. Confirm with
    contributors whether the LSP native support guide
    is still relevant; if not, remove all four
    files together.
